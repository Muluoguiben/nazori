name = "nazori-worker"
main = "src/index.ts"
compatibility_date = "2024-12-18"
compatibility_flags = ["nodejs_compat"]

[ai]
binding = "AI"

[vars]
ENVIRONMENT = "production"

# Gemini API key (primary): `wrangler secret put GEMINI_API_KEY`
# Claude API key (optional): `wrangler secret put CLAUDE_API_KEY`
